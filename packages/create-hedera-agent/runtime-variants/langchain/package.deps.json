{
  "$comment": "Dependency diff applied by the CLI when --framework=langchain. The CLI removes every entry in `remove.dependencies` from template/package.json and merges `add.dependencies` in. Tracked here (not in code) so AgentLab's code generator can read the same file.",
  "framework": "langchain",
  "add": {
    "dependencies": {
      "@hashgraph/hedera-agent-kit-langchain": "1.0.0",
      "@langchain/core": "1.1.24",
      "@langchain/langgraph": "1.2.8",
      "@langchain/openai": "1.2.7",
      "langchain": "1.2.24"
    }
  },
  "remove": {
    "dependencies": [
      "@ai-sdk/openai",
      "@hashgraph/hedera-agent-kit-ai-sdk"
    ]
  }
}
